TRAIBOX — Phase: Modules UX (Prompt 1/3). Objective: Make “My Space” the single Home and merge Dashboard into it. Fix Trade Passport card routing so it opens the canonical Passport tab.

Constraints:
- Do NOT change: Dark/Light mode logic, Operator/Financier switch logic, existing overall layout system.
- Keep design clean & consistent with existing style.

1) Merge Dashboard → My Space (Single Home)
- “Dashboard” should no longer be a separate primary destination. Either:
  A) remove it from the left nav, OR
  B) keep /dashboard route but redirect it to /space (My Space).
- Move the current Dashboard widgets/grid into My Space as a section inside the page:
  Section title: “Overview” (or “Workspace Overview”)
  Include a “Customize” button (reuse existing if available; otherwise stub with a tooltip/toast “Customization coming next”).

2) My Space structure (top-to-bottom)
A) Primary CTA row (very important)
- Primary button: “New Trade” (highest emphasis)
- Secondary: “Load Demo Data” (if exists)
- Responsive: stack on mobile, inline on desktop.

B) KPI row (4 cards)
- Active Trades
- Pending Actions
- Alerts
- Passport readiness (e.g., “Missing items” / “Ready” with status pill)
These should read from whatever demo/store state exists; if not available, keep the same demo numbers currently shown.

C) Next Actions list (3–7 items max)
- Actionable list with small pills and a CTA per row (e.g., “Run compliance checks”, “Request UBO declaration”, “Generate proof pack”, “Request funding”).
- If real data not available, derive from existing demo objects (compliance checks etc) to avoid random placeholders.

D) Continue / Recent Trades
- Keep the “Recent Trades” list. Clicking a trade should open Trade Intelligence with that trade selected.
- Implement minimal context passing using a URL param if needed: /trade-intelligence?tradeId=XYZ.

3) Trade Passport card MUST open canonical Passport route
- There must be ONE canonical passport destination.
- Canonical preference: /compliance-proofs?tab=passport (or whatever existing Compliance & Proofs route is that supports the passport view).
- Update My Space “Trade Passport” card click handler to open that canonical route.
- Ensure there is no alternate route that shows a different passport UI.

4) Acceptance checks
- There is no “two homes” confusion: Dashboard is merged into My Space or redirects to it.
- My Space clearly answers: “what’s happening + what should I do next?”
- Clicking Trade Passport from My Space always lands on the canonical passport tab.

At the end, output:
- Files changed (with paths)
- What changed in each file
- Any route redirects added
